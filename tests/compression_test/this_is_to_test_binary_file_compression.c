#include <stdio.h>

// the test_binary.exe file is compiled to work on my machine, recompile this if it doesnt run


int main(int argc, char const *argv[])
{
    printf("if this is printed, the compression and decompression was successfull");
    return 0;
}
